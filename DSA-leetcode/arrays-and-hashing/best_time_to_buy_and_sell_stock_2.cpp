#include<bits/stdc++.h>
using namespace std;

int maxProfit(vector<int>& prices) {
    int max;

    for (int i = 0; i < prices.size(); i++) {
        int startingIdx = i;
        if (prices[i + 1] > prices[i]) {
            
        }
    }
    return max;
}

int main()
{
    return 0;
}